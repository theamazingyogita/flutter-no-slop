Screenshots for the main README.

- `before.png` — output generated without the skill installed
- `after.png` — output generated with the skill installed, same prompt

Both must come from the same prompt, same model, on the same day. If the
comparison is not honest it is worse than having no images at all.
